
//Este relamente no supe como hacerlo, pero bueno, busque como se podria hacer y puse un ejemplo, pero no haye en mi cabeza como hacer la logica de este ejercico :(
 
/**
ejemplo 1
function invertir(palabra) {
    let palabraInvertida = '';
    for (var i = palabra.length - 1; i >= 0; i--) {
        palabraInvertida += palabra[i];
    }
    return palabraInvertida;
}
let palabra = "Hola mundo";
let palabraInvertida = invertir(palabra);
console.log(palabraInvertida);



ejemplo 2 usando una funcion
function invertirString(palabra) {
    return palabra.split('').reverse().join('');
}
let cadena = "Hola mundo";
let palabraInvertida = invertirString(palabra);
console.log(palabraInvertida); 

*/ 